spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5434/user_db
    username: postgres
    password: postgres123

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

server:
  port: 8081

#jwt:
#  secret: shopsphere-local-secret-key-change-this-before-production
#  expiration: 3600000
